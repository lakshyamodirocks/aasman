# Aasmaan — apni KB (the product explaining itself)
VERSION: 2026-09-06 9057ebe lakshyamodirocks/aasman
GENERATED: 2026-09-06T17:55Z
FROM: README.md docs/{WHY,TRUST,FAQ,ROADMAP,FLAGS}.md CHANGELOG.md ai.py
<!-- generated by gen-selfkb.py — do not edit; edit the docs it reads -->

## Cheat-sheet — the 12 commands that answer 90%
Plain words work first: "agents dikhao", "update yourself", "go offline", "awaaz 30", "pause", "battery", "2+2", "date". Commands are optional.
/help — every command · /capabilities — what THIS install can do right now (measured, trust this over me) · /agents — the experts · /agent auto <task> — picks one
/hands — device control on this machine · /stop (or "ruk") — the brake · /do list — tools · /remember <fact> · /memory · /kb query <q> — your own notes, offline
/attach <file|png|pdf> — give it a file or screenshot · /update — new version (you press it; keys and memory stay) · /setup — installer again · /keys — add a free key
/lang en|hinglish|hi|auto — answer language · /voice (or v) — push-to-talk · /net off — never touch the cloud · /egress — every network call it made · /quit

## Ye kya hai
# Aasmaan (आसमान)

**Free AI on your own device — phone or PC. One program, one file. No account needed. Your data stays with you.**

# Why Aasmaan exists

## Install — per device (exact commands)
**Android (Termux)** — install **Termux from F-Droid** (f-droid.org/packages/com.termux — the Play Store build is old and broken; F-Droid is a free store for open-source apps, install it from f-droid.org), open Termux, then:

```bash
pkg upgrade -y && pkg install -y curl python && curl -fsSL https://raw.githubusercontent.com/lakshyamodirocks/aasman/main/install.sh | bash
```

```bash
curl -fsSL https://raw.githubusercontent.com/lakshyamodirocks/aasman/main/install.sh | bash
```

```powershell
irm https://raw.githubusercontent.com/lakshyamodirocks/aasman/main/install.ps1 | iex
```

## Kaunsa device kya deta hai
| | Android / Termux | Linux · macOS | Windows |
|---|---|---|---|
| `ai` chat, 19 experts, memory, `/do` tools, keyless web | ✓ | ✓ | ✓ |
| local model (Ollama) | ✓ RAM-tiered, wizard picks | ✓ hardware-tiered | ✓ hardware-tiered |
| voice in/out | ✓ Termux:API; whisper.cpp + Piper via `setup-menu` (being verified) | `say` on macOS, espeak on Linux | via PowerShell TTS (recipe) |
| screen-read (Shizuku, no root) | ✓ via `setup-menu` | — | — |
| floating fact-checker bubble | planned (Termux:Float) | — | — |
| daemon (awareness only) | `ai daemon` in tmux / Termux:Boot | systemd --user / LaunchAgent (opt-in) | Task Scheduler (opt-in) |
| uninstall | `cleanup.sh` (dry-run first) | `pc-setup.sh --uninstall` (manifest) | `$env:AI_UNINSTALL=1; irm … \| iex` |

| **phone ↔ computer pairing** (`ai pair`, QR) | phone side: any browser | host side ✓ | host side ✓ |

## Tera data kahan rehta hai — aur kya kabhi nahi hota
| What | Where | Why |
|---|---|---|
| `ai.py` + a launcher | Windows: `%LOCALAPPDATA%\Aasmaan\` · Linux/macOS/Termux: `~/.local/bin/ai` | the program |
| experts + packs | `~/.ai-experts.json`, `~/.ai-experts/` | 18 specialists + `aasmaan`, the product explaining itself |
| tool router, panel | `~/.ai-tools.json`, `~/.ai-panel.html` | `/do` routing, `ai serve` |
| keys (optional) | `~/.ai-env`, readable only by you | cloud brains you *chose* to add |
| memory | `~/ai-vault/` | your notes and recall, plain files |
| PATH | Windows: one folder in **user** PATH, only if you press Enter · Linux/macOS: **never edited** — the line is shown, you add it · Termux: `~/.bashrc` gets the PATH line only | so `ai` runs by name |

- **No server of ours.** There is nothing to host, so nothing to breach on our side.

- **No telemetry.** No crash reports, no "anonymous usage", no pings. The word `analytics` does not appear in the code.

- **No silent installs.** Python and Ollama are installed by you from official installers; every stage waits for Enter.

- **No key exports.** Keys are read from `~/.ai-env` by `ai` itself (`_load_env`); they are not put in your shell or handed to child processes.

- **No unattended action.** The daemon runs with `AI_ATTENDED=0`; forging tools and shell execution are hard-blocked there.

- **No text inside a script.** Device hands (docs/HANDS.md) are code-owned templates; free text is only ever its own argument or stdin, never embedded in an `osascript`/`-Command`/`rish -c` script. A hand that violates this fails to load (`hands_check`), and the daemon may only use read-only hands.

- **No fake results.** If a test did not run, it says so. If no brain can see your image, it says so instead of guessing.

## Bina net, bina key — kya chalta hai
The first thing a new user types is arithmetic, a date, or a conversion. Those never reach a brain: a rung-0 layer answers `2+2`, `15% of 4200`, `date`, `time in Tokyo`, `30 days from today`, `age 16 Nov 1994`, `days until 25 Dec`, `5 km in miles`, `100 f to c`, `2 gb in mb`, `b64 …`, `sha256 …`, `uuid`, `json …`, `pw 20` (never written to the journal), `wa : ` / `upi  ` / `qr ` link and QR makers, `emi` / `sip` / `lumpsum` (formula shown, calculator-only wording, no advice), and with net but no key `mausam Jaipur` (Open-Meteo, attribution printed). The calculator walks an allow-listed syntax tree — never `eval`. `ai tour` runs six of these for real in a minute. Then: keyless web search, scraping and images, the 18 expert packs, memory and KB, and every device hand — all without an account.

Haan. Bina brain ke bhi: `2+2`, `15% of 4200`, `date`, `time in Boston`, `5 km in miles`, `age 16 Nov 1994`, `emi/sip`, links/QR, device hands — `ai tour` 60 sec me dikha deta hai. Aur Ollama + local model = zero account, zero key, zero net. Chat, 18 experts, memory, KB, sab. Net ho to keyless tools bhi (DuckDuckGo search, scrape, Pollinations images). Cloud key sirf tab jab tum khud daalo.

## Brain aur model
The installer picks a **coding model** for what you have (sizes are the download; licences: Apache-2.0 except `qwen2.5-coder:3b`, which is under the Qwen Research licence — fine for personal use, check it before commercial use):

| Your machine | Model | Download |
|---|---|---|
| 4 GB RAM, no GPU | qwen2.5-coder:1.5b | 1 GB |
| 8 GB RAM, no GPU | qwen2.5-coder:3b | 1.9 GB |
| 16 GB RAM, no GPU · or 6–8 GB VRAM · or Apple M-series 16 GB | qwen2.5-coder:7b | 4.7 GB |
| 12 GB VRAM | qwen2.5-coder:14b | 9 GB |
| 16–24 GB VRAM | qwen2.5-coder:32b | 20 GB |
| 32 GB+ RAM or Apple 32 GB | qwen3-coder:30b | 19 GB |

## Update aur uninstall
When we publish a new version, `ai` notices (it fetches this repo's tiny `VERSION` file at most once a day, 3-second cap, opt out with `AI_UPDATE_CHECK=0`) and prints one line under its banner:

```
[ai] update available: 2026-09-10 a1b2c3d  (tera: 2026-09-06 54fb95a)
     chalao:  irm https://raw.githubusercontent.com/lakshyamodirocks/aasman/main/install.ps1 | iex      ya yahin:  /update
```

## Phone ↔ computer pair
Most people with an iPhone also have a Mac or a PC. Run this on the computer:

```
ai pair
```

## Hands, voice, language
`awaaz 30`, `pause`, `next song`, `battery`, `say hello`, `copy: some text`, `torch on`, `screenshot le`, `remind me in 10 min: chai` — in chat, by voice, or `/hand `. `/hands` lists what **this** device can do right now and why the rest is hidden (Termux:API, Shizuku, a desktop session). `/stop` (or "ruk") is the brake; `/undo` puts a state back. Every hand is a code-owned template with typed parameters, a risk letter, and an undo or a stop; free text never enters a script; the daemon may only use read-only hands. What ships per platform, and what the OS does not allow (screen cast, for one): docs/HANDS.md.

`v` (or `/voice`) listens once, then treats the words exactly like typed chat: stop-words first, device hands, plain-word commands, then the brain, and it speaks the answer one sentence at a time so "ruk" lands after the current sentence. Safe commands run at once; non-destructive ones ask *by voice* and take a spoken haan/nahi (silence is no); the destructive tier (`/quit /clear /keys /update /setup /net /bg ! /run /serve`) always needs a typed yes, because a mis-heard Hindi word through an English recogniser is a real failure mode. No wake word, no always-on mic. Transcripts are not written to the journal unless `/voice log on`. Engines are whatever the device has: Android TTS or the setup-menu's kokoro/piper wrapper (and Google STT, which is English-only there; offline Hindi = whisper via setup-menu), `say` on macOS (Hindi voice Lekha if installed), `spd-say`/`espeak-ng` on Linux, System.Speech on Windows, piper anywhere with a model. `ai voice notify` pins a bol/ruk notification on Android. `/voice off` closes everything, including the panel's mic endpoint.

## 19 experts — 18 specialists + Aasmaan (ye KB)
- **18 experts in six groups** — CREATE (image prompts, music, video edit), PUBLISH (Instagram, YouTube, final checks), GROW (marketing, SEO, analytics), THINK (research-verify, writing, planning), BUILD (coding, UI/UX, voice), CARE (finance explain-only, coaching with a crisis protocol, self-OSINT). Each has a persona, refusals, honest weak spots on a small model, five Hinglish exemplars and a knowledge base with dated sources. `/agent auto ` picks; crisis phrasing always reaches the helpline protocol.

Aasmaan (ye pack) product ko khud samjhata hai: install, update, pair, kya shipped hai, kya PLANNED. Device ki haalat (keys, model, RAM) /capabilities batata hai, ye KB nahi.

## Ab kya kaam karta hai (shipped)
- **Runs and is gated on Linux**: a gate of scripted installs from this exact bundle, uninstall, and a golden set of pinned behaviours (`tests/golden.py`). CI repeats golden + install on Linux, macOS and Windows; the badge at the top is the live answer.

- **Android/Termux**: the install path is replayed in tests from this exact bundle; Ollama + local models ran on the maintainer's phone in earlier sessions. whisper.cpp / Piper voice are offered by `setup-menu` and are being verified device by device.

- **Windows**: verified by CI only; no physical Windows machine yet. Your first run is a real test — please report.

- **Hands** (device control): engine and rules are pinned by tests on Linux; the macOS and Windows hands are built from vendor documentation and not yet run on a physical machine — `/hands` marks the unverified ones.

- **Shipped**: the one-file harness, provider ladder, privacy scrub, 19/19 expert packs (incl. the self-KB), memory + KB, `/do` ladder with tool forge and safety scan, `/attach` with vision, chat-to-command, `/capabilities`, self-update, daemon, Telegram helper bot.

- **Planned, not built**: fact-checker floater, family profiles, key lock/unlock in the hardware keystore, speaker recognition, using your own ChatGPT/Claude subscription through the vendor's CLI (researched; Anthropic's terms block it, OpenAI's are grey), iOS thin client.

- Beta means worked on in the open. If a line here reads stale, that is the feedback loop's job: say so.

## PLANNED — abhi bana NAHI hai (future tense me chhupa ke mat bolna)
PLANNED: fact-checker floater, family profiles, key lock/unlock in the hardware keystore, speaker recognition, using your own ChatGPT/Claude subscription through the vendor's CLI (researched; Anthropic's terms block it, OpenAI's are grey), iOS thin client.
PLANNED: Peer mode between two full installs (`ai pair --peer`, `ai peer ask|send`) once per-peer capabilities exist — a peer token is a seat, not a channel (docs/PAIRING.md)
PLANNED: **First physical Windows run** by a real user; fix whatever CI could not see
PLANNED: **Keys in the OS keychain** (macOS `security`, Linux secret-service, Windows Credential Manager) instead of a `0600` file; on Android, `termux-keystore` wrapping with an auto-relock timer and a manual lock/unlock
PLANNED: **Family profiles** on shared phones: a picker at start, per-profile memory, consent-based sharing between devices
PLANNED: **Fact-checker floater** on Android (Termux:Float + user-initiated OCR with pinned model files)
PLANNED: `format_check.py` for publishing experts (deterministic caption/limit validator)
PLANNED: Devanagari-script onboarding; screen-reader pass; localisation beyond Hindi

## Poori command list 1 (/agent … /model)
/agent — EXPERTS: /agent auto <task>  (naam yaad na ho to khud chunta hai)
/agents — EXPERTS: /agents
/ask — BRAIN: /ask <brain> <q>
/attach — SYSTEM: /attach <file>
/auto — BRAIN: /auto /online /local
/bg — VOICE: /voice on|off|stop|log on|status|notify  — push-to-talk; safe c
/budget — MEMORY: /budget <n>
/cache — MEMORY: /cache on|off|clear
/calc — TOOLS0: mausam Jaipur  — offline, bina brain; /calc <expr>
/canary — SYSTEM: /canary
/capabilities — SELF: /capabilities (ye install abhi kya kar sakta hai)
/clear — ANSWER: /clear
/connect — OpenAI-compatible endpoint as a brain: /connect <url> [model] [key] (L
/corpus — LEARN: /corpus [export [redact] [path]]  — har jawab ka archive (datas
/ctx — MEMORY: /ctx <files>
/device — SYSTEM: /device
/do — /trace [<goal>]                   — jo /do chal gaya wo agli baar ka e
/egress — SELF: /egress (har network call ka log: kahan, kab, kitna)
/embed — SYSTEM: /embed <text>
/exit — same as /quit
/explain — SYSTEM: /explain
/group — EXPERTS: /group
/hand — run one device hand by id (/hands lists them)
/hands — HANDS: /hands
/help — this command list
/impact — IMPACT: /impact <action> [target]  — kya chhuega, kaun depend karta ha
/json — ANSWER: /json <q>
/kb — MEMORY: /kb build|query <q>
/keys — VOICE: /voice on|off|stop|log on|status|notify  — push-to-talk; safe c
/lang — LANG: /lang [en|hinglish|hi|auto]  — English by default; installer ask
/list — CONNECT: /list [add|rm] — plain: "pdf ka connector chahiye", "shopping
/local — BRAIN: /auto /online /local
/mcp — CONNECT: /mcp find <service|use-case> (catalogue, offline)
/memory — MEMORY: /memory
/metrics — BRAIN: /metrics [reset]
/mode — ANSWER: /mode
/model — BRAIN: /model <name>

## Poori command list 2 (/models … /wish)
/models — MODELS: /models (Ollama me kya hai: chat/vision/embed, kaun attached)
/net — VOICE: /voice on|off|stop|log on|status|notify  — push-to-talk; safe c
/online — BRAIN: /auto /online /local
/panel — BRAIN: /panel %s
/plan — TUNING: /plan <goal> (steps: expert/tool/hand/brain, dikha ke, poochh 
/privacy — SYSTEM: /privacy [on|off|<text>]
/q — same as /quit
/quit — VOICE: /voice on|off|stop|log on|status|notify  — push-to-talk; safe c
/remember — MEMORY: /remember <fact>
/route — BRAIN: /route <q>
/run — SYSTEM: /run <cmd>
/save — ANSWER: /save
/serve — SYSTEM: /serve [port]
/setup — VOICE: /voice on|off|stop|log on|status|notify  — push-to-talk; safe c
/short — ANSWER: /short
/stop — the brake: stop speech, hands, background jobs
/tags — MEMORY: /tags
/tool — DO: /tool <name> <what it should do>
/tour — TOOLS0: /tour
/trace — /trace [<goal>]                   — jo /do chal gaya wo agli baar ka e
/tuning — TUNING: /tuning (tier ke knobs: persona/kb/answer/plan)
/undo — put the last device hand's state back
/update — VOICE: /voice on|off|stop|log on|status|notify  — push-to-talk; safe c
/usage — TUNING: /usage [days] (asli token counts per brain)
/version — SELF: /version
/voice — VOICE: /voice (ya sirf  v ) = ek baar suno
/why — BRAIN: /why
/wish — DO: /wish [run|clear]

## Switches (env vars, ~/.ai-env)
`AI_BRAND` — default `Aasmaan` — product name in UI/panel/manifest (:13)
`AI_VAULT` — default `~/ai-vault` — notes/journal/KB root (:45)
`AI_LOCAL_MODEL` — default `qwen3:4b-instruct-2507-q4_K_M` — local model tag (:64)
`AI_LOCAL_CTX` — default auto by RAM (4k–32k) — forces `num_ctx` (:130)
`AI_VISION_MODEL` — default unset — vision model for Ollama; also *enables* the local vision path (:139,:148)
`AI_PRIVACY` — default `1` — **`0` disables `redact()` on every cloud send** (:200,:300,:1394)
`AI_FORCE_OFFLINE` — default unset — `1` → `net_up()` False (:258). **Not a network switch — F2**
`AI_AUTOWEB` — default unset (on) — `0` disables auto web-search (:1375)
`AI_REPO` — default auto (install dir) — where experts/agents/panel are found (:1147)
`AI_VOICE` — default `1` — `0` = voice off at start: no TTS, no mic, `/api/listen` refuses
`AI_LANG` — default unset (→ `en`, then auto-mirror) — `en` / `hinglish` / `hi` pins the answer language + the translated UI strings; written by the installers, `/lang` shows/
`AI_ATTENDED` — default `1` — **`0` hard-blocks forge + shell** (:1640; forced 0 by daemon :2608, telegram :2849)
`AI_SERVE_HOSTS` — default `""` — extra Host values the rebind guard accepts (:3334)
`AI_SERVE_TOOLS` — default unset — **`1` lets HTTP callers run `/do`, which dispatches through a shell** (:3511)

## Kisne banaya, kyun (who made it, why)
Free AI keeps shrinking: free tiers get cut, logins get killed, projects relicense — we watched it happen more than once while building this. Meanwhile a huge number of people carry a genuinely capable phone, or have a laptop at home, and nothing beyond a chatbot's daily limit. Aasmaan is free AI for the device you already own — phone, laptop, or both paired together: not a lighter version of something bigger, the real thing — local where it can be, reaching free-tier clouds only when you choose to add a key.

"Beta" here doesn't mean broken and left alone. It means worked on, out loud, in the open. Feedback is read at least weekly; the day it lands it gets tagged by how urgent it is, and anything at the top of that ranking gets looked at immediately, not queued behind a roadmap. If something in this document reads stale by the time you read it — a model got discontinued, a number moved — that's expected, not a broken promise. Say so, and it gets fixed. That loop is the actual reason this can be trusted to stay useful, not just useful on day one.

— Lakshya, Jaipur

## Community aur bug
Telegram group · Discord — humans, questions, show-and-tell. Bugs and feedback go to GitHub issues so nothing is lost. The Telegram helper bot runs on the maintainer's own device (no server) and answers `/install`, `/faq`, `/version`, forwards `/feedback`; free-text answers are off by default. See docs/COMMUNITY.md.

This is a beta that is updated in place. Open a bug or feedback issue (templates ask for `ai version`; never paste a key), or start a Discussion. Feedback is read weekly and tagged by severity; the top rank gets immediate attention. Security problems go to a private advisory.

## Sources
README.md, docs/WHY.md, docs/TRUST.md, docs/FAQ.md, docs/ROADMAP.md, docs/FLAGS.md, CHANGELOG.md, ai.py (HELP, KNOWN_CMDS, dispatcher) — generated by gen-selfkb.py at build; never hand-edited.
